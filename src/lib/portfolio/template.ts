export function generatePortfolioConfig(data: any) {
  // This function will be implemented based on your portfolio needs
  return data;
}

export const TEMPLATE_STYLES = `
  .container-custom {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }
  
  .section-padding {
    padding: 4rem 0;
  }
  
  .gradient-text {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
`; 